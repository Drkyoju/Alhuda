{
  "name": "Alhuda quiz PWA",
  "install": "npm ci && npx playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "dev-server",
      "command": "python3 -m http.server 8765",
      "description": "Static dev server for the PWA at http://127.0.0.1:8765 (Playwright BASE_URL)."
    }
  ],
  "ports": [
    {
      "name": "dev-server",
      "port": 8765
    }
  ]
}
