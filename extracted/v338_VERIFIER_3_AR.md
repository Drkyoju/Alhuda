# Verifier 3 — تحقق عدائي مستقل بعد v337 (لايف)

**المنشور:** v339 (إصلاحات V3؛ كسر كاش `citation-canonical-v338.js`)  
**اللايف عند بدء التحقق:** https://alhuda-zi6bbd.cranl.net/ = **alhuda-v337**

## خلاف مع حالة اللايف (v337) — ما التقطه V3 وخفي عن A/B

| # | العيب | الدليل | الإصلاح |
|---|------|--------|---------|
| 1 | OCR ظاهر في الاستشهاد: **ش يء** | `613367ce` تمائم، `b2f4d852` تولـة، `42842c86` جحود — معروضة عبر المسار الحي | `ش يء`→`شيء` في البنك + الكانون + `postFixCitationPhrases` |
| 2 | OCR: **يف اللوح / هم حبسنة / حسنة اكملة / بني ذلك** | `850c6a73` و`10a36d97` — نص معروض حرفياً على مسار الاستشهاد | إصلاح ميكانيكي مطابق لنافذة كتاب المعلم (OCR) → `في اللوح` / `هم بحسنة` / `حسنة كاملة` / `بيّن ذلك وفصّله` |
| 3 | **عدم تطابق استشهاد↔سؤال** | `0a39f855` سؤال «رخص من الرقى ما خلا من…» واستشهاد تعريف عام للشرك («صرف ش يء من العبادة») | سُحب من الكانون ومُسح `source_quote` — بلا اختراع بديل |
| 4 | **~198** استشهاداً وهمياً تحت عنوان الكتاب | صياغات ميتا: «هو المعنى الصحيح المذكور في شرح الكتاب…»، «هذه من فوائد حديث…»، «هو ما ثبت في لفظ الحديث/الأثر…» | `isInventedMetaCitation` + إسقاط من العرض (لا اختراع نص كتاب) |
| 5 | فلتر الجواب يبتلع اقتباساً كتابياً صحيحاً | `rejectAsAnswerOnly` كان يرفض أي استشهاد **يحتوي** نص الخيار | تضييق الشرط: الرفض لقرب طول الجواب فقط |
| 6 | إعراب نطقي فاسد | `حَدِيثٍ صَحِيحٌ` / `حَدِيثٍ حُسْنُ` / `غَيْرَ صَحِيحٌ` / `يسقط حُقُوقُ` في `speech-diacritics-map.js` | `حَدِيثٌ صَحِيحٌ` / `حَدِيثٌ حَسَنٌ` / `غَيْرُ صَحِيحٍ` / `يُسْقِطُ حُقُوقَ` |

## ما وافق عليه V3 من مزاعم اللايف (v337)

| البند | الحكم |
|------|--------|
| «جاهزة» مخفية | **صح** — `setQuranReciteStatus` يفرّغ ويُخفي عند ready |
| Mute TTS للاستشهاد الكتابي | **صح** — `buildFeedbackSpeechPlan` |
| صح/خطأ → `صَحْ`/`خَطَأْ` + `sah.mp3`/`khata.mp3` | **صح** — مسار `tf0`/`tf1` |
| HEARTS_ENABLED = false | **صح** |
| expectQuestionId | **صح** |
| 36 بقايا بلا مصدر كتابي متجاور | **مقبول** — لم تُختَرع نصوص |

## أرقام العرض (محاكاة مسار `findBookCitation` + شرح نظيف)

- قبل إصلاح الميتا: ~579 «معروض» كثيره ميتا غير كتابي  
- بعد V3: استشهاد كتابي أو شرح غير ميتا فقط؛ **0** تسرّب `ش يء`/`يف اللوح`/ميتا على المسار

## اقتباسات مقطوعة (… ) — ملاحظة بلا اختراع

بقي اختصار مقصود في بعض الشروح (`… إلخ`، `يا عبادي…`) — لم تُوسَّع من مصدر متجاور أنظف من OCR الكتاب. لم يُختَرع متن.

## ملفات

- `app.js` — فلاتر + postFix + rejectAsAnswerOnly  
- `citation-canonical-v338.js` (+ مزامنة المحتوى)  
- `questions-bank.json` / `.js` / `-v311.js`  
- `speech-diacritics-map.js`  
- `extracted/v338_v3_display_suspects.json` (مسح ما قبل الإصلاح)

## سياسة V3

لا اختراع نص كتاب. لا Cloudflare. الاستشهاد يظهر عند وجود نص نظيف؛ بلا TTS للاستشهاد.
