name: Bake missing TTS (Fish Audio)

# DEPRECATED — live Fish TTS only. Old tts-baked MP3s were removed from the repo.
# Do not re-enable unless restoring offline baked mode intentionally.
on:
  workflow_dispatch:

jobs:
  bake:
    runs-on: ubuntu-latest
    steps:
      - name: Baked TTS disabled
        run: |
          echo "Baked TTS is disabled. Lesson speech uses live Fish (FISH_VOICE_ID + s2-pro)."
          echo "Quran ayahs use Hudhaify CDN — not tts-baked."
          exit 0
